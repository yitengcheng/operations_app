export const boxShadow = {
  borderRadius: 8,
  borderWidth: 1,
  borderColor: '#f3f3f3',
  elevation: 1.5,
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 0 },
  shadowOpacity: 1,
  shadowRadius: 1.5,
};
export const divider = (height: number) => {
  return {
    height: height ?? 10,
    backgroundColor: '#d9d9d9',
  };
};
